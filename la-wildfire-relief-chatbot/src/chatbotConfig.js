const config = {
    initialMessages: [{ type: "text", content: "Hello! How can I assist you with LA wildfire relief today?" }],
    // Add more configuration options as needed
  };
  
  export default config;